{
  "manifest_version": 3,
  "name": "Consent Load-Order Scanner",
  "version": "0.3.3",
  "description": "Scans the current site for cookies and tags, detects the consent manager, checks whether it loads before cookies/tags are placed, attributes each tag/cookie to hardcoded HTML or a tag manager, and tests whether tracking actually stops after consent is withdrawn.",
  "permissions": [
    "webRequest",
    "webNavigation",
    "cookies",
    "scripting",
    "storage",
    "browsingData",
    "tabs"
  ],
  "host_permissions": ["<all_urls>"],
  "background": {
    "service_worker": "background.js"
  },
  "action": {
    "default_popup": "popup.html",
    "default_title": "Consent Load-Order Scanner"
  }
}
